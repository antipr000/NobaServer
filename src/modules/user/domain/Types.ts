export enum PaymentMethodType {
  CARD = "Card"
}

export enum VerificationStatusType {
  NOT_STARTED = "NotStarted",
  PENDING = "Pending",
  REJECTED = "Rejected",
  FLAGGED = "Flagged",
  VERIFIED = "Verified"
}